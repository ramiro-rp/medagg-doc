# Общее описание продукта – Medical Imaging Dataset Aggregator

## 1. Назначение

Исследователям и врачам часто нужны **кастомные наборы медицинских изображений**
для обучения и оценки моделей машинного обучения. Такие данные разбросаны по
разным платформам (Kaggle, PhysioNet, открытые репозитории клиник и т.п.) и
имеют различные форматы и структуру.

**Medical Imaging Dataset Aggregator** – это веб-приложение, которое:

- централизует **метаданные** об открытых наборах данных;
- предоставляет **единый интерфейс поиска**;
- позволяет пользователю **сформировать и скачать собственный набор данных**
  по заданным клиническим и техническим критериям.

В рамках MVP система ориентирована на **торакальную визуализацию**
(рентген и КТ грудной клетки), но архитектура позволяет расширять её на другие
области и модальности.

---

## 2. Целевая аудитория

- 🧑‍⚕️ **Врачи** (радиологи, пульмонологи), участвующие в исследованиях;
- 🧪 **Инженеры по машинному обучению / data scientists**, работающие
  с медицинскими изображениями;
- 🎓 **Студенты**, изучающие ML и медицинскую визуализацию.

Предполагается базовая техническая грамотность
(работа с ZIP-архивами, CSV-файлами), но не обязательно навыки программирования.

---

## 3. Основные цели пользователя

1. **Найти наборы данных**, релевантные клиническому или исследовательскому вопросу.
2. **Отфильтровать** наборы по модальности, анатомической области, типу ML-задачи и тегам.
3. **Объединить данные из нескольких источников** в единый согласованный набор.
4. **Скачать** изображения и **таблицу метаданных**, готовую для использования
   в ML-проекте.
5. **Повторно воспроизвести** ранее сформированные наборы с помощью истории запросов.

---

## 4. Ключевой функционал (область MVP)

### 4.1 Каталог наборов данных

Система поддерживает централизованный **каталог наборов данных** в PostgreSQL.

Для каждого набора хранятся как минимум:

- идентификатор, название и краткое описание;
- ссылка на внешний источник;
- анатомическая область (например, грудная клетка);
- модальность (рентген, КТ, МРТ и др.);
- тип ML-задачи (классификация, сегментация, детекция и т.п.);
- количество исследований / изображений;
- примерный размер;
- теги (например, «пневмония», «COVID-19», «перелом»);
- путь к локальной копии (если есть);
- даты создания и последнего обновления записи.

### 4.2 Поиск и фильтрация

Поиск по каталогу выполняется с помощью:

- **свободного текстового запроса** (ключевые слова на русском или английском);
- **структурированных фильтров**:
  - модальность;
  - анатомическая область;
  - тип ML-задачи;
  - теги.

В результатах поиска для каждого набора отображаются:

- краткое описание и источник;
- основные характеристики (количество изображений, размер);
- оценка релевантности (если используется модуль поиска);
- элементы управления для выбора наборов при формировании коллекции.

### 4.3 Формирование набора (коллекции)

На основе результатов поиска пользователь может запросить формирование
**кастомной коллекции**:

1. Система отбирает исследования / изображения, соответствующие запросу и фильтрам.
2. Для поддерживаемых источников система:
   - скачивает или получает доступ к нужным файлам;
   - сохраняет их в локальном файловом хранилище;
   - формирует **ZIP-архив**, включающий:
     - файлы изображений;
     - **CSV-файл** с метаданными (ID, метки, модальность, исходный набор и т.п.).
3. Система сохраняет **запрос пользователя** и связывает его с созданной коллекцией.

### 4.4 Аккаунт и история

После аутентификации пользователь получает доступ к личному разделу, где он может:

- просматривать **историю своих коллекций**;
- видеть статус формирования (в процессе / готово / истёк);
- скачивать сформированные архивы (пока они доступны);
- при необходимости пересоздавать истёкшие коллекции (если реализовано).

### 4.5 Функции администратора (минимальный набор)

Администратор может:

- создавать / редактировать / деактивировать записи в каталоге наборов;
- управлять справочниками (модальности, анатомические области, типы задач, теги);
- просматривать логи и запросы пользователей.

---

## 5. Обзор архитектуры

Архитектура MVP включает:

- **Backend** – Django + Django REST Framework  
  REST-API для аутентификации, каталога, поиска, коллекций и админ-операций;
  хранение данных в PostgreSQL.

- **Модуль поиска** – отдельный Python-модуль `medagg-search`  
  Парсит текстовые запросы (RU/EN) с использованием таксономии в YAML,
  может вычислять релевантность и извлекать структурированные слоты
  (модальность, область, патология, тип задачи).

- **Frontend** – Vite + React + Ant Design  
  Одностраничное приложение (SPA), общающееся с backend через REST-API.

---

## 6. Среда и развёртывание (MVP)

MVP рассчитан на запуск на **одном Linux-хосте** с использованием Docker:

- контейнер backend-а (Django + DRF);
- контейнер PostgreSQL;
- при необходимости контейнер или сервис для раздачи собранного фронтенда;
- локальное файловое хранилище для ZIP-архивов.

В качестве хоста могут использоваться:

- ноутбук разработчика;
- простой сервер или VPS;
- компактное устройство (например, Raspberry Pi 4 с SSD) для демонстрации.

Подробности описаны в `deploy-guide.ru.md`.

---

## 7. За пределами MVP

Потенциальные направления развития (не входят в MVP):

- детальные «паспорта» для каждого набора данных в каталоге;
- загрузка наборов пользователями и модерация;
- рекомендательная система на основе истории запросов и скачиваний;
- интеграция со сторонними инструментами разметки;
- распределённое / облачное развёртывание с масштабированием и мониторингом.
